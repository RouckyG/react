import React from 'react';

export class Unfold extends React.Component{

    constructor(props) {
        super(props);
    }
    state ={
        display : true,
    }
    
    this.state(oldState => ({display(oldState display)}))
}